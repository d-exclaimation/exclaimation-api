type Post {
    id: ID!
    title: String!
    body: String!
    crabrave: Int!
    snippet: String!
    nodes: [PostNode!]!
}

input PostDTO {
    title: String!
    body: String!
}

type PostNode {
    type: String!
    leaf: String!
}